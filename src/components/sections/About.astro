<!-- ===== ABOUT ===== -->
<section id="about" class="about-section">
  <!-- Edge-to-edge separator -->
  <div class="full-screen-separator"></div>

  <div class="container">
    <p class="eyebrow">ABOUT</p>
    <h2 class="h2 h2--light-purple">WHAT IS JUNCTIONX LISBON?</h2>

    <!-- Just the facts, 2-3 lines -->
    <p class="about-text">
      An international 36-hour hackathon where teams of 1 to 5 people unite to build technology with real-world impact. Choose from challenges set by our industry partners, from AI to Tech for Society, and turn your wildest ideas into reality over one weekend.
    </p>

    <!-- Retro text-based timeline instead of boxes -->
    <div class="retro-timeline">
      <div class="phase">
        <span class="step-num">01</span>
        <span class="step-name">Brainstorm</span>
      </div>
      
      <div class="arrow">---></div>
      
      <div class="phase">
        <span class="step-num">02</span>
        <span class="step-name">Prototype</span>
      </div>
      
      <div class="arrow">---></div>
      
      <div class="phase">
        <span class="step-num">03</span>
        <span class="step-name">Build</span>
      </div>
      
      <div class="arrow">---></div>
      
      <div class="phase">
        <span class="step-num">04</span>
        <span class="step-name">Present</span>
      </div>
    </div>
  </div>
</section>

<style>
  .about-section {
    position: relative;
    background-color: transparent;
    color: #ffffff;
    /* Very tight padding to keep sections close */
    padding-bottom: clamp(30px, 5vw, 60px);
    padding-top: 0;
  }

  /* Full screen width glowing line */
  .full-screen-separator {
    width: 100%;
    height: 2px;
    background: #BDBDFF;
    box-shadow: 0 0 10px rgba(189, 189, 255, 0.3);
    margin-bottom: clamp(30px, 5vw, 50px);
  }

  .container {
    max-width: 1050px;
    margin: 0 auto;
    padding: 0 clamp(20px, 5vw, 40px);
  }

  .h2 {
    font-size: clamp(2rem, 4vw, 3.5rem);
    margin: 0 0 1.5rem 0;
    line-height: 1.1;
  }

  .about-text {
    color: #e0e0e0;
    font-size: clamp(1.1rem, 2vw, 1.25rem);
    line-height: 1.6;
    max-width: 900px;
    margin: 0 0 3rem 0;
  }

  /* Timeline styling */
  .retro-timeline {
    display: flex;
    align-items: center;
    gap: clamp(10px, 2.5vw, 40px);
    font-family: monospace;
    color: #BDBDFF;
  }

  .phase {
    display: flex;
    flex-direction: column;
    gap: 4px;
  }

  .step-num {
    font-size: 0.85rem;
    opacity: 0.5;
    letter-spacing: 2px;
  }

  .step-name {
    font-size: clamp(1.1rem, 2.5vw, 1.5rem);
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 1px;
    color: #ffffff;
  }

  .arrow {
    font-size: clamp(1rem, 2vw, 1.5rem);
    opacity: 0.4;
    font-weight: bold;
    /* Pushes the arrow slightly down to align better with the text */
    margin-top: 15px; 
  }

  /* Mobile responsiveness */
  @media (max-width: 768px) {
    .retro-timeline {
      display: none;
    }
  }
</style>