<!-- ===== VIDEO ===== -->
  <section class="video-hero" id="top">
    <video class="video" autoplay muted loop playsinline preload="metadata">
      <source src="./assets/video.mp4" type="video/mp4">
    </video>
    <div class="overlay"></div>
    <div class="container content">
      <p class="eyebrow">10th and 11th October 2026 · Lisbon, Portugal</p>
      <h1 class="h1">
        <span class="nowrap">
          Junction
          <img class="logo-inline" src="./assets/junctionx-wordmark-small.png" alt="JunctionX">
        </span>
        Lisbon
      </h1>
      <p class="lead">Build in 36h with mentors & partners. Win prizes.</p>
      
      <div class="actions">
        <a class="btn" href="https://form.typeform.com/to/WWtZ6KRT" target="_blank" rel="noopener">APPLY NOW</a>
        <a class="btn btn--ghost" href="agenda.html">See agenda</a>
      </div>

      <!-- Updated Discord Text Link -->
      <div class="discord-wrapper">
        <a class="discord-link" href="https://discord.gg/yourlink" target="_blank" rel="noopener">
          Join our Discord <span class="discord-arrow">→</span>
        </a>
      </div>

    </div>
  </section>

<style>
  .discord-wrapper {
    /* Adds the requested padding to the top and left */
    margin-top: 16px; 
    padding-left: 12px; 
    display: flex;
    justify-content: flex-start;
  }

  .discord-link {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    color: #ffffff;
    /* Increased font size to match the standard buttons */
    font-size: 16px; 
    text-decoration: none;
    /* Bulletproof underline method */
    border-bottom: 1px solid transparent; 
    padding-bottom: 2px;
    transition: color 0.2s ease, border-bottom-color 0.2s ease;
  }

  .discord-link:hover {
    color: #BDBDFF;
    /* This forces the underline to appear on hover */
    border-bottom-color: #BDBDFF; 
  }

  .discord-arrow {
    display: inline-block;
    transition: transform 0.2s ease;
  }

  .discord-link:hover .discord-arrow {
    transform: translateX(4px);
  }

  /* Keeps it centered if mobile view kicks in for the buttons */
  @media (max-width: 900px) {
    .discord-wrapper {
      justify-content: center;
      padding-left: 0;
    }
  }
</style>